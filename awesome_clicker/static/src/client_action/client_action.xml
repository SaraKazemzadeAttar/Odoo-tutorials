<templates>
	<t t-name="awesome_clicker.client_action">
		<div>
			<span>Clicks: <ClickValue/></span>
			<button class="btn btn-primary ms-1" t-on-click="() => this.clicker.increment(9)">Increment</button>
		</div>
		<t t-if="clicker.level >= 1">
			<div>
			<p>Bots: <t t-esc="clickBots"/></p>
			<div class="card">
				<div class="card-header">
					<t t-esc="this.clicker.clickBots"/>x ClickBots (10 clicks/10seconds)
					<i class="fa fa-android"></i>
				</div>
				<div class="card-body">
					<button
							t-on-click="() => this.clicker.buyClickBot()"
							class="btn btn-primary"
							t-att-disabled="this.clicker.clicks &lt; 1000"
					>
						Buy ClickBot (1000 clicks)
					</button>
				</div>
			</div>
			</div>
		</t>
	</t>
</templates>